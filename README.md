# nvim-ui-mcp
nvim-ui-mcp lets AI agents **see, interact with, and verify the actual Neovim UI** through MCP.  Unlike existing Neovim MCP servers, `nvim-ui-mcp` attaches as a real Neovim UI client and observes the rendered screen.
